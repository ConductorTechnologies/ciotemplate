{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "pip build",
            "type": "shell",
            "command": ". ~/.virtualenvs/cmc/bin/activate && cd $DEV/conductor/ciotemplate &&  pip install --upgrade . --target=$HOME/Conductor",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "tox",
            "type": "shell",
            "command": "tox",
            "group": "test"
        }
    ]
}